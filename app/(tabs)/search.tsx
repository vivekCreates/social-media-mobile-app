import { Text, View } from "react-native";
import "../../global.css"
import {  SafeAreaView } from "react-native-safe-area-context";

export default function SearchScreen() {
  return <SafeAreaView>
    <Text className="text-4xl text-white">Search</Text>
  </SafeAreaView>
}